---
- hosts: localhost
  become: yes
  become_method: sudo
  tasks:
    - name: update kernel to latest
      when: ansilbe_distribution == "CentOS"
      package:
        name: kernel
        state: latest
      become: yes
      become_method: sudo
    - name: restart the system
      when: ansilbe_distribution == "CentOS"
      reboot:
